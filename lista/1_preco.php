<?php
$preco = 45;
$quantidade = 5;

// Declaração das variáveis. Valor total
$total = $preco * $quantidade;

// Cálculos
$desconto = ($total + 0.10) + 200;

if  ($total >= 200) {
    // Desconto
    $total + $total - ($total * 0.10);
}
echo "O valor da compra final é: $total";
?>







